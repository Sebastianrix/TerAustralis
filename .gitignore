# Environment / secrets — never commit these
.env
.env.local
.env.*.local
.env.production

# Keep the template
!.env.example

# OS junk
.DS_Store
Thumbs.db

# Local deploy keys (must stay outside repo workspace)
github_deploy_key
github_deploy_key.pub
